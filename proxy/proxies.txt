# Сюда докидывайте прокси — по одному в строку, формат:
# ip:port:user:pass
#
# Пример:
# 31.59.20.176:6754:jnascuuh:0k6d5094dihq
#
# Строки с # и пустые строки игнорируются.
# После добавления перезапустите бота (npm start) или CLI (npm run reg):
# при старте каждый прокси проверяется на живость, мёртвые отсеиваются.
